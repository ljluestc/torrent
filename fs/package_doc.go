// Package torrentfs provides a FUSE filesystem for accessing torrents.
// 
// IMPORTANT: All files in this directory MUST use the "torrentfs" package name, not "fs".
// This is to avoid confusion with the standard library's "fs" package and maintain consistency.
package torrentfs